# Netlify configuration — static site, no build step
[build]
  publish = "."

# Long-cache static assets (they are versioned by content/filename)
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000"
